[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "computationaloncology-compute-instructions"
authors = [{name = "Roy Oelen", email = "r.oelen@umcg.ln"}]
dynamic = ["v0.1", "Computational Oncology compute instructions"]
